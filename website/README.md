# AWM Project Website

这是 Agent Workspace Manager 的独立静态介绍页，不参与 Gradle 构建。

## 本地预览

在仓库根目录运行：

```powershell
python -m http.server 4173 --directory website
```

打开 <http://127.0.0.1:4173/> 即可预览。

截图素材位于 `assets/screenshots/`，来自当前运行中的 Compose Desktop 应用。
