# 日常使用指南

Agent Workspace Manager（AWM）用于把一个研发任务准备成明确、可追溯的本地 Agent 工作区。它管理任务、服务工作区、Git 风险状态、`AGENTS.md` 和当前唯一的交付动作：Tag。

## 适用场景

- 一个需求需要同时修改多个服务；
- 研发使用 IDEA、WebStorm、Codex 或 Cursor 在本地开发；
- 需求来自飞书，团队按组（业务线）管理服务；
- 提交测试环境前需要创建可追溯的 Tag。

AWM 隔离的是代码工作目录和 Git 工作区；端口、数据库、Docker、凭据和外部测试环境仍由项目本身管理。

## 首次设置

1. 首次启动会自动创建 `~/awm/tasks` 并将它设为任务根目录，不需要手动选择。每个任务会在该目录下创建一个子目录；如需调整，可在设置页选择新目录。
2. 使用“添加仓库”选择本地 Git 主仓库。可多选目录；非 Git、Bare 仓库、临时 Linked Worktree 和重复仓库会被跳过。
3. 在组中添加服务并设置显示名称、基础分支、工作区策略及需要时的 Tag 规则。
4. 可选：填写 IDE、终端和任务完成后默认打开的工作区工具。
5. 可选：在“Meegle”中选择项目。创建任务时会查询已配置空间中属于当前登录用户的进行中 Sprint 需求。

未创建额外组时，应用保留一个默认组，并在任务与服务页面隐藏组层级。

如果当前任务根目录已有任务，更换目录后会先显示迁移确认。迁移期间请关闭相关 IDE 和终端；AWM 会保留脏文件和未跟踪文件、修复标准 Worktree 注册，并在全部校验成功后切换配置。失败会自动恢复旧目录；若只剩旧目录因文件占用无法删除，迁移仍然生效并显示待清理路径。

## 创建研发任务

1. 点击“创建研发任务”。
2. 可选地手工填写需求编号或飞书需求链接，或从候选需求中选择。输入需求时，资料目录使用下一步填写的文件夹名称；不会替换该名称。设置中的资料根目录和资料子目录均已配置时，创建页会只读预检并显示预计新建或复用的完整资料路径；任一项未配置时，创建流程不会展示或尝试创建资料目录。
3. 填写“任务名称 / 文件夹名称”。它会原样成为任务目录名，不能包含 Windows 非法字符、保留设备名、末尾点或空白。
4. 确认任务分支。组的默认前缀可包含 `{num}`；它会从需求编号、需求链接或普通文本中的最后一段数字解析。
5. 选择本任务需要修改的服务。服务搜索只过滤展示，不会清除已选服务。
6. 在右侧填写任务人工说明，或预览最终 `AGENTS.md`。选择工作区工具后点击创建。

创建期间可以继续查看任务和复制信息；只有会与当前 Git 或配置写入冲突的动作会被禁用。

## 服务与工作区策略

### 标准 Worktree

适用于需要为任务创建独立 Feature 分支的服务。创建时会从最新已获取的基础远程引用创建 Worktree，不会切换或移动主 checkout。每个模块始终获得独立物理 Worktree；单模块沿用任务分支，多模块默认使用“任务分支-模块名”，创建时可分别搜索或手工覆盖基础分支和目标分支。

### 独立克隆

适用于需要直接在固定远程分支工作的服务。它从 `origin` 完整克隆并检出选定分支，不额外创建 Feature 分支、Worktree 或执行 Bootstrap。多个任务同时使用同一远程分支时仍可能产生业务层面的代码冲突，团队需要自行协调。

## 任务详情与 Git 状态

每个工作区显示：

- `无未提交` 或 `N 个文件未提交`：包含 staged、unstaged、冲突和未跟踪文件，不包含 ignored 文件；
- `已推送` 或 `N 个提交未推送`：只比较本地已知 upstream，不执行 fetch；
- `检查中`：正在读取本地 Git 状态，不会 fetch 或切换分支；
- `工作区不存在`、`不是有效的 Git 工作区`、`Git 仓库身份不匹配`、`分支不一致`、`Detached HEAD` 或 `存在进行中的 Git 操作`：对应行会展示完整原因和修复图标。

点击修复图标会先展示预计动作。失效的普通 Worktree 或独立克隆目录会先改名保留为同级备份；仅远程存在任务分支时必须明确确认复用。

任务详情可逐个复制当前实际分支和路径、打开 IDE/终端/文件夹、查看工作数据、添加服务，以及在满足开关条件时构建 Tag。分支设置可隐藏头部中如 `master`、`develop` 的精确分支名，但工作区行仍完整展示。创建任务时，分支输入框可搜索所选标准 Worktree 服务的远程 Heads；下拉查询不会执行 fetch 或读取 Tag。

启动后，AWM 会在后台静默查找尚未填写路径的开发工具。Windows 只检查 Program Files、`%LOCALAPPDATA%\Programs`、JetBrains Toolbox 已知目录和 PATH；macOS 只检查 `/Applications`、`~/Applications`、JetBrains Toolbox 已知目录和 PATH，不会联网或递归扫描磁盘。手动保存的路径优先级最高，即使当前无效也不会被自动结果覆盖；未找到时不会提示。

开发工具中的“临时选择开发工具”默认关闭。关闭时，工作数据和每个服务都直接使用其默认工具打开；开启后才显示临时选择菜单。任务创建完成不会因为这个开关自动打开服务。

Git 设置页只读取本机 Git 可执行文件、版本、用户配置来源、凭据助手名称、已配置仓库的当前分支、HEAD、upstream、ahead/behind、状态、Worktree 和原始远程 URL，不执行任何联网 Git 命令。

## AGENTS.md

说明分为三层：

```text
~/awm/agents/global/AGENTS.md
~/awm/agents/groups/<groupId>/AGENTS.md
<任务目录>/AGENTS.md
```

设置页可直接编辑全局或组说明，也可以在对应文件路径用外部编辑器修改。任务级文件保留人工说明区；保存任务说明时，系统区会按最新任务、全局说明和组说明重新生成。外部编辑与未保存本地编辑冲突时，必须选择磁盘版本或本地版本。

## Tag 与归档

Tag 只在组开关和服务模块/独立克隆子开关都开启时可用。构建前会执行 Git 风险检查和临时合并预检；不会 Force Push、自动 Rebase 或自动解决冲突。详细规则见 [安全与 Tag 流程](SAFETY-AND-TAG-FLOW.md)。

归档或删除前会检查未提交内容、未推送提交和进行中的 Git 操作。强制归档可能丢弃未保存文件，只应在确认风险后使用。
